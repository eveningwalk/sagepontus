아래는 **입력**이다. 출력은 이 형식을 복사한 것이 아니라, 다른 LLM에 붙일 **한 덩어리 지시문(문단형)**이다.

--- 입력 ---
{{ qa_pairs }}
--- 끝 ---

{% if domain_focus %}
도메인: {{ domain_focus }} (문장에 자연스럽게 녹일 것)
{% endif %}

**생성 규칙**  
- 문단 2~5개, 한국어, **서술형 지시문**만.  
- 금지: 질문·답변 줄줄이 나열, `질문:`/`답변:` 라벨, 입력 번호 재현.  
- 포함: 역할·맥락·목표·출력 형식·제약·(필요 시) 데이터·검증 — 답변에 근거한 사실만.

출력은 **프롬프트 본문만** (한국어).
