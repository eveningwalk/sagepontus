아래는 **입력** 설문 원문이다. 출력은 설문 복사본이 아니라, 다른 LLM에 붙일 **한 덩어리 지시문**이다.

--- 사용자 답변 ---
{{ qa_pairs }}
--- 끝 ---

{% if domain_focus %}
도메인: {{ domain_focus }}
{% endif %}

한 편의 프롬프트에 반드시 포함:
- 역할 / 사용자 맥락(답변에 근거한 사실만) / 목표
- 출력: 실행 계획 + 측정·검증 항목(지표·기한 등 가능하면)
- 제약: 답변에 없는 사용자 속성 가정 금지

금지: 질문·답변만 줄줄이 나열하는 형태를 본문 전부로 하기.

출력은 **프롬프트 본문만** (한국어).
