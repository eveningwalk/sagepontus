아래는 **입력용** 설문 원문이다. 출력은 이 형식을 그대로 반복하는 것이 아니라, 다른 LLM에 **그대로 붙여 넣을 한 덩어리 지시문**이어야 한다.

--- 사용자 답변 ---
{{ qa_pairs }}
--- 끝 ---

{% if domain_focus %}
도메인 초점(참고): {{ domain_focus }}
{% endif %}

반드시 다음을 **연속된 한 편의 프롬프트**로 포함하라 (소제목 라벨은 써도 되나, 설문지처럼 질문·답변만 나열하지 마라):
1) 역할: 다운스트림 AI가 맡을 역할
2) 사용자 맥락: 위 답변에서만 인용·요약한 사실
3) 목표: 무엇을 생성·분석·제안하도록 할지
4) 출력 형식: 글머리, 단계, 길이 등
5) 제약: 금지·우선순위

금지: 질문 번호·분류·질문 문장·답변만 순서대로 나열하는 것을 본문 전부로 하기. "아래는 설문입니다" 등 메타 설명.

한국어로, **완성된 프롬프트 텍스트만** 출력하라.
