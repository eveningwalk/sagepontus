아래는 **입력**이다. 출력은 다른 LLM의 시스템 프롬프트로 붙여넣을 **구조화된 컨텍스트 프롬프트**다.

--- 입력 ---
{{ qa_pairs }}
--- 끝 ---

{% if domain_focus %}
도메인: {{ domain_focus }}
{% endif %}

**생성 규칙**
- `# 역할과 상황`, `# 목표`, `# 기대 응답 방식` 섹션은 반드시 포함한다.
- `# 제약`은 답변에 제약이 명시된 경우에만 포함한다.
- 각 섹션은 1~3문장으로 압축한다.
- 답변에 없는 내용은 추가하지 않는다.

출력은 **섹션 본문만** (한국어).
